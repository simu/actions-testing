on:
  workflow_dispatch: {}
  push:
    branches:
      - master
  schedule:
    - cron: '*/2 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: find cause
        run: echo $GITHUB_EVENT_NAME
